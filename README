This bot is using MySQL database. So you need to create database and three tables in there.


CREATE TABLE TASKS (
  id INT PRIMARY KEY AUTO_INCREMENT,
  Name VARCHAR(255)
);

CREATE TABLE USERS (
  id INT PRIMARY KEY AUTO_INCREMENT,
  Name VARCHAR(255)
);

CREATE TABLE RESULTS (
  TaskID INT,
  UserID INT,
  Score INT,
  FOREIGN KEY (TaskID) REFERENCES TASKS(id),
  FOREIGN KEY (UserID) REFERENCES USERS(id)
);
